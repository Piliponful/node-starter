version: "3.2"
services:
  client:
    build: ./client
    volumes:
      - type: volume
        source: dist
        target: /root/app/client/dist

  server:
    build: ./server
    volumes:
      - type: bind
        source: ${PWD}/server/log
        target: /root/app/server/log
    depends_on:
      - mongodb
    environment:
      - SECRET=${SECRET}
      - SENDGRID_API_KEY=${SENDGRID_API_KEY}
      - MONGODB_ROOT_PASSWORD=${MONGODB_ROOT_PASSWORD}

  nginx:
    build: ./nginx
    volumes:
      - type: volume
        source: dist
        target: /usr/share/nginx/html
    ports:
      - 80:80
      - 443:443
    depends_on:
      - server

  mongodb:
    image: bitnami/mongodb
    ports:
      - 27017:27017
    environment:
      - MONGODB_ROOT_PASSWORD=${MONGODB_ROOT_PASSWORD}
    volumes:
      - mongo:/bitnami

volumes:
  dist:
  mongo:
